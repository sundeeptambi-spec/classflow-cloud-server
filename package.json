{
  "name": "classflow-cloud-server",
  "version": "0.1.0",
  "private": true,
  "description": "Railway PostgreSQL server for Class Flow registration, activation, panel usage, and Q&A cache.",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node src/server.js"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "wrangler": "^4.125.0"
  },
  "dependencies": {
    "pdf-parse": "^2.4.5",
    "pg": "^8.23.0"
  }
}
